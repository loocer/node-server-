#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_blessed@0.1.81@blessed/bin/tput.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_blessed@0.1.81@blessed/bin/tput.js" "$@"
  ret=$?
fi
exit $ret
