#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_yamljs@0.3.0@yamljs/bin/json2yaml" "$@"
  ret=$?
else 
  node  "$basedir/../../../_yamljs@0.3.0@yamljs/bin/json2yaml" "$@"
  ret=$?
fi
exit $ret
